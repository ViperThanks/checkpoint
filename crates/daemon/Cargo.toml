[package]
name = "agent-aspect-daemon"
version = "0.1.0"
edition = "2024"
license.workspace = true

[[bin]]
name = "agent-aspectd"
path = "src/main.rs"

[dependencies]
agent-aspect-core = { path = "../core" }
serde_json = "1"
chrono = "0.4"
uuid = { version = "1", features = ["v7"] }
